# Documentation

Lives in the `documentation` folder as a list of typst files. Compile them all as a pdf using the following command: 

```
typst compile documentation\main.typ --root . frontend\assets\Annotator-manual.pdf
```